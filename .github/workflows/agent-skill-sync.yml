name: Agent Skill Sync

on:
  pull_request:
  push:

jobs:
  parity-check:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Check agent/skill parity
        run: bash scripts/check_agent_skill_sync.sh
